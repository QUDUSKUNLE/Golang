package events

const (
	// USER_EVENTS is the topic for user-related events.
	USER_EVENTS = "user-events"

	// DIAGNOSTIC_EVENTS
	DIAGNOSTIC_EVENTS  = "diagnostic-events"
	DIAGNOSTIC_CREATED = "diagnostic-created"
	DIAGNOSTIC_UPDATED = "diagnostic-updated"
	DIAGNOSTIC_DELETED = "diagnostic-deleted"

	// Notification events
	NOTIFICATION_EVENTS = "notification-events"
)
